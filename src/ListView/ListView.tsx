import React from "react";
import { View, Text } from "react-native";

interface ListViewProps {}

const ListView = () => {
  return (
    <View style={{ backgroundColor: "rgba(255,0,0,0.2)", flex: 1 }}>
      <Text />
    </View>
  );
};

export default ListView;
